<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo/Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <div class="container mb-3">
        <div class="d-flex justify-content-between sticky-top py-2 bg-white" >
            <a href="" class="fw-bold fs-2 login-heading" style="text-decoration: none;">Todoist</a>
            <form action="/logout" method="post">
                <button type="submit" class="logout-btn mt-2">Logout</button>
            </form>
        </div>
    </div>
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                    <div class="">
                        <h1 class="fw-bold fs-3 mb-3">All Users</h1>
                        <%if(userDetails.length !=0) {%>
                        <table class="table table-card" style="box-sizing: border-box;">
                            <thead>
                              <tr>
                                <th scope="col">S.N</th>
                                <th scope="col">Name</th>
                                <th scope="col">Email</th>
                                <th scope="col">Manage</th>
                              </tr>
                            </thead>
        
                            <%for(let i=0; i<userDetails.length; i++) {%>
                            <tbody>
                              <tr>
                                <th scope="row"> <%=i+1%> </th>
                                <td> <%= userDetails[i].name %> </td>
                                <td>
                                    <form action="/user-details" method="post">
                                        <input type="hidden" name="email" value="<%= userDetails[i].email %>">
                                        <button class="btn p-0 text-primary" ><%= userDetails[i].email %> </button>
                                    </form>
                                </td>
                                <td>
                                    <form action="/delete-user" method="post">
                                        <input type="hidden" name="email" value="<%= userDetails[i].email %>">
                                        <button class="btn p-0 text-danger" >Delete User</button>
                                    </form>
                                </td>
                              </tr>
                            </tbody>
                            <%}%>
                            <%} else{%>
                                <h1 class="noUser text-center fw-bold fs-3 py-3 m-0">No User Exists</h1>
                                <%}%>
                          </table>
                    </div>
            </div>
        </div>
    </div>
</body>

</html>